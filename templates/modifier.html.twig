<!DOCTYPE html>
<html lang="en">

<head>
    <title>Bootstrap Example</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="{{ asset('styles/bootstrap4/bootstrap.min.css')}}">
    <script src="{{ asset('styles/bootstrap4/bootstrap.min.js')}}"></script>
    <script src="{{ asset('js/jquery-3.3.1.min.js')}}"></script>
    <script src="{{ asset('styles/bootstrap4/popper.js')}}"></script> 
</head>

<body>
<br><br><br>
    <div class="container">
        <h2>Mdification :</h2>
        <form action="{{path('Mod')}}">
            <input type="hidden"  name="id" value="{{Livre.id}}">
            <div class="form-group">
                <label for="email">Nom:</label>
                <input name="nom" type="text" class="form-control"  placeholder="{{Livre.nom}}" name="email">
            </div>
            <div class="form-group">
                <label for="email">Description:</label>
                <input name="description" type="text" class="form-control"   placeholder="{{Livre.description}}" name="email">
            </div>
            <div class="form-group">
                <label for="email">cout:</label>
                <input name="cout" type="number" class="form-control"   placeholder="{{Livre.cout1}}" name="email">
            </div>
            <div class="form-group">
                <label for="email">Remise:</label>
                <input name="remise" type="number" class="form-control"   placeholder="{{Livre.remise}}" name="email">
            </div>
            <div class="form-group">
                <label for="email">Categorie: {{Livre.getIdCategori.getnom}}</label><br>
                <select class="selectpicker" name="categorie">
                    {% for categorie  in categories %}
                    <option value="{{categorie.id}}">{{categorie.nom}}</option>
                    {% endfor %}
                </select>
            </div>
             
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>

</body>

</html>